{
  "scripts": {
    "prettier:check": "prettier --check .",
    "prettier:fix": "prettier --write .",
    "prepare": "husky"
  },
  "lint-staged": {
    "frontend/**/*.{js,ts,jsx,tsx,json,css,scss,md}": "prettier --write",
    "backend/**/*.{js,ts,json,md}": "eslint --fix"
  },
  "dependencies": {
    "@commitlint/cli": "^20.1.0",
    "@commitlint/config-conventional": "^20.0.0"
  },
  "devDependencies": {
    "lint-staged": "^15.5.2",
    "prettier": "3.6.2",
    "prettier-plugin-tailwindcss": "^0.7.1"
  }
}
